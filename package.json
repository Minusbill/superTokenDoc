{
  "name": "supertoken-docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy:cf": "npm run docs:build && wrangler pages deploy docs/.vitepress/dist --project-name supertoken-docs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
